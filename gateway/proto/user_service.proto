syntax = "proto3";

package  pb;

option go_package = "./proto;pb";

service UserService {
  rpc UserService(TestRequest) returns (TestResponse){}
  rpc TestUser(TestRequest) returns (TestResponse){}
}

message TestRequest{
  int32 id = 1;
}

message TestResponse{
  string msg = 1;
}